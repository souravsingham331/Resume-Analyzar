# Database Connection
DATABASE_URL="file:./dev.db"

# Auth Secret (JWT / Session signing key)
AUTH_SECRET="resume_ai_super_secret_auth_key_32bytes_minimum_length_production_grade"

# AI Provider Configuration: 'mock', 'openai', or 'gemini'
AI_PROVIDER="mock"
AI_API_KEY=""

# File Storage Configuration: 'local' or 's3'
STORAGE_PROVIDER="local"
STORAGE_PATH="./uploads"

# Application Settings
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"
